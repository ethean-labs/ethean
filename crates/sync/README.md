# ethean-sync

Parent/range sync planners, hysteretic duty gate, backfill invariants, and explicit checkpoint trust (Phase 11).

Does not persist blocks (see `ethean-storage`) and does not dial peers (see `ethean-network`).
