# ethean-node

Node library crate. Holds legacy modules moved from the former root `src/` during Phase 02 workspace migration.

Depends on `ethean-primitives` and `ethean-profile`. The production binary lives in [`../../bin/ethean`](../../bin/ethean/).
