# crypto

Node re-exports [`ethean-crypto`](../../crypto/): Lean XMSS wire types (52-byte pubkey, 2536-byte signature).

Legacy BLS and local WOTS modules were deleted in Phase 07. Production verify fails closed until the pinned leanSig backend builds.
