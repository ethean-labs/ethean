# optimization

Optional performance extras (caching, monitoring helpers, experimental tuners). Keep this crate from leaking into consensus correctness.

The subcrate can be built on its own (`Cargo.toml` in this folder). Treat ML/cache/dashboard pieces as optional; they are not part of Lean Consensus correctness.
