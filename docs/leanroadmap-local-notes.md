# leanroadmap.org notes

Lean R&D is tracked on the whole site (overview, benchmarks, devnets, research, clients, calls), not only the research-tracks heading. Linked specs and interop plans get copied into local `bazalinacaklar/` markdown when we actually need them. Cursor rules stay gitignored; this file is just the public pointer.
