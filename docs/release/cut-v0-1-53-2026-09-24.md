# Cut v0.1.53 milestone release (2026-09-24)

## Why

Workspace version had already advanced through `0.1.49`–`0.1.53` (live FC
store, FC-driven head, vote ingest, fork_choice HTTP, admin events poll) plus
earlier leanMultisig / leanMetrics / multi-platform binary work still sitting
under `[Unreleased]`. Operators needed a tagged GitHub Release.

## What landed

| Piece | Detail |
| --- | --- |
| [`CHANGELOG.md`](../../CHANGELOG.md) | `[Unreleased]` cut into `## [0.1.53] - 2026-09-24` |
| Tag | Annotated `v0.1.53` on the release commit |
| GitHub Release | Notes from the changelog section; binaries via `release-binaries` workflow |

## Scope of 0.1.53 (vs 0.1.47)

- leanMultisig / `ethean-prover`, signed gossip admission, SSZ aggregate fix
- leanMetrics schema v3 + interop dashboard
- Multi-platform release archives (`ethean` + `ethean-prover`)
- Live `ForkChoiceStore`, FC-driven tip, durable rebuild, vote ingest
- `GET /lean/v1/chain/fork_choice` and `GET /lean/v1/events`
- Solo local-finality durable persist

## Follow-ups

- Keep appending operator-visible bullets under `[Unreleased]` on each bump
- External backlog (A2/A3 digests, fixture re-fill, Hive matrix) stays outside
  the repo
