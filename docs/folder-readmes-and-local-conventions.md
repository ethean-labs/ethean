# Folder READMEs and local conventions

Each code/docs directory now has a short README describing what lives there. Local Cursor rules stay in `.cursor/` (gitignored). Lean research notes stay in `bazalinacaklar/` (also gitignored), copied from the Lean Consensus research tracks page so protocol work has a local starting point.
