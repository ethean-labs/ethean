# Folder READMEs and local conventions

Each code/docs directory now has a short README describing what lives there. Local Cursor rules stay in `.cursor/` (gitignored). Lean notes stay in `bazalinacaklar/` (also gitignored), pulled from the whole [leanroadmap.org](https://leanroadmap.org/) surface when a task needs a spec, devnet pin, or paper.
