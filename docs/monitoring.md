# Monitoring (Lean)

Metrics prefix: `ethean_` (`ethean-metrics`).

HTTP health and Lean routes: `ethean-rpc` under `/lean/v1/…` only.

Prometheus scrape wiring and production dashboards remain behind the Phase 12/13 open gates; do not revive Beacon metric names or `/eth/v1` health aliases.
