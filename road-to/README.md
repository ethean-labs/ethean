# road-to

Older planning documents for the Beam/Lean path (vision, phase summaries, crypto/storage reports). Prefer `docs/` and `bazalinacaklar/` for new work; keep this folder for historical context.
