# Apply Ethean onto an ethereum/hive checkout (PowerShell)

```powershell
# From the Ethean repo root:
.\tools\hive\apply-ethean-client.ps1 -HiveRoot G:\path\to\hive
```

Requires Git Bash or WSL for the `.sh` script; this wrapper invokes bash when
available, otherwise prints the manual copy checklist.
