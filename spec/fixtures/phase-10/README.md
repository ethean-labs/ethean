# Phase 10 fixtures

Network topic, Snappy mode-confusion, and Status vectors.

Upstream leanSpec networking fixtures remain deferred; local unit coverage is in `ethean-network-wire` and `ethean-network`.
