# Phase 02 fixtures

Provenance and expected positive/negative cases for the workspace, primitives, and profile migration.

- Manifest: [manifest.toml](./manifest.toml)
- Lock: [`../../pins/phase-02.lock.toml`](../../pins/phase-02.lock.toml)
- Protocol surface: [`../../pins/protocol-surface.toml`](../../pins/protocol-surface.toml)

No large fixture tarball is required for Phase 02. Runtime checks live as unit tests in `ethean-primitives` and `ethean-profile`.
