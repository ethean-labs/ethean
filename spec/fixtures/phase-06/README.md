# Phase 06 fixtures — fork choice

Provenance for local unit coverage of the **modified 3SF-mini / lstar** fork-choice
store (`ethean-fork-choice`). Generation is **not Goldfish** (Phase 00).

Authority: leanSpec `0b7d33ec` (`fork_choice.py`, `timeline.py`, `containers/store.py`).

Upstream binary vectors are not vendored here; see `manifest.toml`.
