# Phase 07 fixtures

XMSS size and signer-safety vectors.

Upstream leanSpec / leanSig byte vectors are deferred until the leanSig backend compiles.
Local unit tests in `ethean-crypto` and `ethean-validator` cover wire lengths and journal invariants.
