# Phase 08 fixtures

Type-1 / Type-2 aggregation vectors.

Upstream leanVM proof byte-diff is deferred until the FFI backend is wired.
Local unit tests cover statement shape, participant order, and statement-bound test aggregates.
