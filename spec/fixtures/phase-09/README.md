# Phase 09 fixtures

Duty gate, tick dedupe, and role-separated signing vectors.

Upstream leanSpec duty timing fixtures remain deferred; local unit coverage lives in `ethean-validator` and `ethean-node` modules.
