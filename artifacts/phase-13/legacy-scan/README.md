# Legacy scan reports

Generated by `tools/release/legacy-scan.sh`.

- `hard-hits.txt` — must be `CLEAN` (BLS crates under `crates/` / `bin/`)
- `hits.txt` — soft retirement symbols (intentional refusal strings allowed)
