[package]
name = "system-info"
version = "0.1.0"
edition = "2021"
description = "Windows-compatible leanVM system-info (Ethean vendor overlay)"
license = "MIT"
publish = false

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
libc = "0.2"
