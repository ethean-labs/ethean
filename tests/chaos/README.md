# Chaos scenarios (Phase 13)

Ignored integration tests until QUIC + RocksDB backends land:

- Peer partition / heal
- Resource exhaustion (disk full, FD pressure)
- Process kill at flush / prove / sign boundaries
