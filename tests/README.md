# tests

Workspace-level tests (interop, security, recovery) that span multiple crates.

- `interop/` — cross-crate and peer-differential suites
- `security/` — signer rollback / conflict matrices
- `recovery/` — crash and journal restart scenarios
